diff --git a/build.xml b/build.xml
index 252d3c4..7430fd9 100644
--- build.xml
+++ build.xml
@@ -79,6 +79,7 @@
             value="${build.c++}/examples/pipes"/>
   <property name="build.docs" value="${build.dir}/docs"/>
   <property name="build.javadoc" value="${build.docs}/api"/>
+  <property name="build.javadoc.timestamp" value="${build.dir}/javadoc.timestamp" />
   <property name="build.encoding" value="ISO-8859-1"/>
   <property name="install.c++" value="${build.dir}/c++/${build.platform}"/>
   <property name="install.c++.examples" 
@@ -788,7 +789,21 @@
     <fail message="'forrest.home' is not defined. Please pass -Dforrest.home=&lt;base of Apache Forrest installation&gt; to Ant on the command-line." />
   </target>
 	
-  <target name="javadoc" description="Generate javadoc">
+  <target name="javadoc-uptodate">
+    <uptodate property="javadoc.is.uptodate">
+      <srcfiles dir="${src.dir}">
+        <include name="**/*" />
+        <!-- These files are auto-regenerated by the init target every time -->
+        <exclude name="contrib/chukwa/bin/VERSION" />
+        <exclude name="docs/cn/src/documentation/sitemap.xmap" />
+        <exclude name="docs/cn/uming.conf" />
+      </srcfiles>
+      <mapper type="merge" to="${build.javadoc.timestamp}" />
+    </uptodate>
+  </target>
+
+  <target name="javadoc" description="Generate javadoc" depends="javadoc-uptodate"
+      unless="javadoc.is.uptodate">
     <mkdir dir="${build.javadoc}"/>
     <javadoc
       overview="${core.src.dir}/overview.html"
@@ -828,6 +843,7 @@
        <group title="contrib: Index" packages="org.apache.hadoop.contrib.index*"/>
 
     </javadoc>
+    <touch file="${build.javadoc.timestamp}" />
   </target>	
 
   <target name="jdiff.check" unless="jdiff.home">
